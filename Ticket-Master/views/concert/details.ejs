<%- include ("../partials/header.ejs"); %>

<!-- Content for the Details page -->
<div class="container">
    <div class="row">
        <div class="offset-md-3 col-md-6">
            <h1 style="margin-top: 3em"><%= title %></h1>

                <div class="form-group">
                    <label style="margin-bottom: 0.5em"  for="concertTextField">Concert Name</label>
                    <strong><%= Concert.name %></strong>
                </div>
                <div class="form-group">
                    <label style="margin-bottom: 0.5em"  for="stateTextField">Concert State:</label>
                    <strong><%= Concert.state %></strong>
                </div>
                <div class="form-group">
                    <label style="margin-bottom: 0.5em"  for="priceTextField">Concert Price Per Ticket:</label>
                    <strong><%= Concert.price %></strong>                    
                </div>           
                <a href="/concert/list" class="btn btn-warning">
                    <i class="fas fa-undo"></i> 
                    Cancel
                </a>

            </form>
        </div>

    </div>
</div>

<%- include ("../partials/footer.ejs") %>